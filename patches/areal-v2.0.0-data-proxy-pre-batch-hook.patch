diff --git a/areal/v2/inference_service/data_proxy/__main__.py b/areal/v2/inference_service/data_proxy/__main__.py
index e0458be..a2755b3 100644
--- a/areal/v2/inference_service/data_proxy/__main__.py
+++ b/areal/v2/inference_service/data_proxy/__main__.py
@@ -5,6 +5,7 @@
 from __future__ import annotations

 import argparse
+import os

 import uvicorn

@@ -12,6 +13,7 @@ from areal.infra.utils.http import (
     get_default_uvicorn_kwargs,
     validate_admin_api_key,
 )
+from areal.utils.dynamic_import import import_from_string
 from areal.utils.logging import suppress_http_loggers
 from areal.utils.network import format_hostport
 from areal.v2.inference_service.data_proxy.app import create_app
@@ -104,7 +106,20 @@ def main():
         chat_template_type=args.chat_template_type,
     )
     suppress_http_loggers()
-    app = create_app(config)
+    hook_import_path = os.environ.get("AREAL_PRE_BATCH_HOOK", "")
+    pre_batch_export_hook = (
+        import_from_string(hook_import_path) if hook_import_path else None
+    )
+    if isinstance(pre_batch_export_hook, type):
+        pre_batch_export_hook = pre_batch_export_hook()
+    if pre_batch_export_hook is not None and not callable(pre_batch_export_hook):
+        raise TypeError(
+            "AREAL_PRE_BATCH_HOOK must resolve to a callable or zero-argument class"
+        )
+    app = create_app(
+        config,
+        pre_batch_export_hook=pre_batch_export_hook,
+    )
     uvicorn.run(
         app,
         host=config.host,
diff --git a/areal/v2/inference_service/data_proxy/app.py b/areal/v2/inference_service/data_proxy/app.py
index 5f703d7..2969196 100644
--- a/areal/v2/inference_service/data_proxy/app.py
+++ b/areal/v2/inference_service/data_proxy/app.py
@@ -4,10 +4,12 @@ from __future__ import annotations
 
 import asyncio
 import hmac
+import inspect
 import json
 import uuid
+from collections.abc import Awaitable, Mapping
 from contextlib import asynccontextmanager
-from typing import Any
+from typing import Any, Protocol
 
 import httpx
 from fastapi import FastAPI, HTTPException, Request
@@ -53,6 +55,20 @@ from areal.v2.inference_service.vllm.bridge import VLLMBridgeBackend
 logger = logging.getLogger("InferenceDataProxy")
 
 
+class PreBatchTrajectoryExportHook(Protocol):
+    """Observe one exported trajectory before it loses interaction identity."""
+
+    def __call__(
+        self,
+        *,
+        session_id: str,
+        trajectory_id: int,
+        interactions: Mapping[str, InteractionWithTokenLogpReward],
+        discount: float,
+        style: str,
+    ) -> Awaitable[None] | None: ...
+
+
 # =============================================================================
 # Response models
 # =============================================================================
@@ -313,7 +329,11 @@ async def _ready_trajectory_loop(app: FastAPI) -> None:
         await asyncio.sleep(0.1)
 
 
-def create_app(config: DataProxyConfig) -> FastAPI:
+def create_app(
+    config: DataProxyConfig,
+    *,
+    pre_batch_export_hook: PreBatchTrajectoryExportHook | None = None,
+) -> FastAPI:
     """Factory that creates the FastAPI app with lifespan-managed resources."""
 
     @asynccontextmanager
@@ -731,15 +751,26 @@ def create_app(config: DataProxyConfig) -> FastAPI:
                 continue
 
             try:
-                _, interactions = session.export_trajectory(
+                exported_trajectory_id, interactions = session.export_trajectory(
                     discount=body.discount,
                     style=body.style,
                     trajectory_id=body.trajectory_id,
                 )
-                merged.update(interactions)
             except KeyError:
                 continue

+            if pre_batch_export_hook is not None:
+                hook_result = pre_batch_export_hook(
+                    session_id=sid,
+                    trajectory_id=exported_trajectory_id,
+                    interactions=interactions,
+                    discount=body.discount,
+                    style=body.style,
+                )
+                if inspect.isawaitable(hook_result):
+                    await hook_result
+            merged.update(interactions)
+
         if all(v.has_tensor_data for v in merged.values()):
             traj = concat_padded_tensors([v.to_tensor_dict() for v in merged.values()])
             traj = RTensor.remotize(traj, node_addr=config.serving_addr)
